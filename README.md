# PR screenshots: Matchday at the Meadow Ground

Images for the pull request from `feat/football-matchday`, rendered with the real renderer on the
development server (a pinned-clock harness and frame-by-frame contact sheets). This branch holds
no code and is never merged; it only keeps the pictures in the PR description alive.

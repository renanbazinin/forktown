# PR screenshots: The turning year

Images for the pull request from `feat/turning-year`, rendered with the real renderer on the
development server (`/tests/manual/seasons.html` and a pinned-clock harness). This branch holds
no code and is never merged; it only keeps the pictures in the PR description alive.

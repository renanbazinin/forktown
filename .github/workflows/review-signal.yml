name: Contribution review signal
on:
  pull_request_review:
    types: [submitted, edited, dismissed]
permissions: {}
jobs:
  signal:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Review changed; trusted policy will re-evaluate current approvals.'
